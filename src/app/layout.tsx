export const metadata = { title: 'Maat Money', description: 'Tech 4 Humanity Finance MCP' };
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <html lang="en"><body>{children}</body></html>;
}
